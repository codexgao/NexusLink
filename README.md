# NexusLink - 智能网址收藏夹

NexusLink 是一个基于 React 和 Google Gemini AI 构建的现代极简主义网址收藏管理工具。它不仅仅是一个简单的链接列表，更是您的个人知识网关。

## ✨ 核心功能

*   **🤖 AI 智能识别**: 输入网址，利用 Google Gemini 模型（配合 Google Search Grounding）自动抓取并分析网站的标题、描述、分类和标签。
*   **👍 互动投票系统**: 为您喜欢的工具点赞或标记不喜欢的链接，建立个人偏好档案。
*   **🎨 现代化 UI 设计**: 
    *   流光溢彩的渐变交互设计。
    *   完美适配深色/浅色模式（支持系统自动跟随）。
    *   响应式网格布局。
*   **🔍 快速检索**: 支持通过标题、描述、标签或 URL 进行实时搜索和过滤。
*   **📂 自动分类**: 基于 AI 分析结果自动归类，支持顶部标签栏快速筛选。
*   **💾 本地持久化**: 数据存储在浏览器 LocalStorage 中，无需服务器即可保存您的数据。

## 🛠️ 技术栈

*   **前端框架**: React 19
*   **开发语言**: TypeScript
*   **样式库**: Tailwind CSS
*   **图标库**: Lucide React
*   **AI 模型**: Google Gemini API (`gemini-2.5-flash`)
*   **SDK**: @google/genai

## 🚀 快速开始

### 1. 环境配置

本项目依赖 Google Gemini API 进行网址分析。

1.  获取 API Key: 访问 [Google AI Studio](https://aistudio.google.com/) 获取您的 API 密钥。
2.  配置环境: 确保运行环境中包含环境变量 `API_KEY`。

### 2. 启动项目

如果您是在本地环境运行：

```bash
# 1. 安装依赖
npm install

# 2. 启动开发服务器
npm start
# 或
npm run dev
```

打开浏览器访问本地地址（通常为 `http://localhost:3000` 或 `http://localhost:5173`）。

### 3. 功能说明

*   **添加书签**: 点击右上角的"添加新网址"按钮，输入 URL，点击"AI 识别"自动填充信息，或手动输入。
*   **管理**: 支持删除不需要的书签。
*   **投票**: 点击书签卡片右下角的拇指图标进行评价。

## ☁️ 部署 (Vercel)

本项目可以轻松部署到 Vercel 平台。

1.  **代码托管**: 将代码推送到 GitHub、GitLab 或 Bitbucket 仓库。
2.  **创建项目**: 登录 [Vercel](https://vercel.com/)，点击 **"Add New..."** -> **"Project"**。
3.  **导入仓库**: 选择你刚刚推送的 Git 仓库并点击 **"Import"**。
4.  **配置环境变量**:
    *   在 **"Environment Variables"** 部分展开配置。
    *   添加一个新的变量：
        *   Key: `API_KEY`
        *   Value: 您的 Google Gemini API Key
5.  **开始部署**: 点击 **"Deploy"** 按钮。等待构建完成后，即可访问您的在线网址收藏夹。

## 📁 目录结构

*   `App.tsx`: 应用主入口与状态管理。
*   `components/`: 
    *   `BookmarkCard.tsx`: 单个书签展示卡片组件。
    *   `AddBookmarkModal.tsx`: 添加书签的模态框组件。
*   `services/`:
    *   `geminiService.ts`: 与 Google Gemini API 交互的逻辑封装。
*   `types.ts`: TypeScript 类型定义。
*   `constants.ts`: 初始演示数据与常量配置。

---

*Personal Gateway to the Digital World.*
